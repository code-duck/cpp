#include<iostream>
#include<cmath>

int main()
{
    double x = 3.14;
    double y = 4;
    double z;

    // z = std::max(x, y); finding the maximum of two numbers
    // z = std::min (x, y); finding the minimum of two numbers

    // z = std::pow(2, 4);  2 raised to the power of 4

    // z = sqrt(9); this tells the square root of 9

    // z = abs(-5); this tgive the +ve of the no. and tells how far the number is from 0

    // z = round(3.14);  this rounds the number to the nearest integer

    // z = ceil(3.14);   this rounds the number to the next integer 

    // z = floor(3.14);  this ropunds the number to the previous integer


    std::cout<< z;
}